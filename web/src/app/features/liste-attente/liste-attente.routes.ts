import { Routes } from '@angular/router';
import { FeaturePlaceholderComponent } from '../../shared/feature-placeholder.component';

export const LISTE_ATTENTE_ROUTES: Routes = [{ path: '', component: FeaturePlaceholderComponent, data: { title: 'Liste d’attente' } }];
