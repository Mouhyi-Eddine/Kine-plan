import { Routes } from '@angular/router';
import { ApiContractGapComponent } from '../../shared/api-contract-gap.component';

export const PLATEFORME_ROUTES: Routes = [{ path: '', component: ApiContractGapComponent, data: { title: 'Espace plateforme' } }];
